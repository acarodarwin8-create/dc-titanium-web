# Open Graph (redes sociales)

Tamaño OBLIGATORIO: exactamente 1200x630px (.jpg).

- `og-image.jpg` — imagen principal para compartir
- `og-curso-etabs.jpg`
- `og-curso-revit.jpg`

Nota: `src/app/layout.tsx` usa hoy `/Logo_V8_Premium_Serio.png` como imagen
Open Graph. Para usar `og-image.jpg` aquí, hay que actualizar
`openGraph.images` en `layout.tsx` (archivo existente — pedir confirmación
antes de tocarlo).
